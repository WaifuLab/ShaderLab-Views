import { Extension } from "../state/index.js";
/** Extension that enables a placeholder—a piece of example content to show when the editor is empty. */
export declare function placeholder(content: string | HTMLElement): Extension;
